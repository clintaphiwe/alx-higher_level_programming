#include <stdio.h>
#include <ctype.h>

// int base, exponent;
// int power=1, expo;

// printf("Enter the base: ");
// scanf("%d", &base);
// printf("Enter the exponent: ");
// scanf("%d", &exponent);

// expo = exponent;
// if (exponent > 0){
//     while (exponent != 0){
//         power = power * base;
//         exponent--;
//     }
//     printf("%d to the power of %d is %d", base, expo, power);
// }

printf("Hello World");